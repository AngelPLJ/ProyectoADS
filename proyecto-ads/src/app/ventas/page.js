// `app/page.js` is the UI for the `/` URL
export default function Page() {
    return <nav className="flex min-h-screen flex-col items-center justify-between p-24">
        <h1>¡Bienvenido a la página de ventas!</h1>
    </nav>
  }